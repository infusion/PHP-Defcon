# This is a comma testing configuration file, for use in test cases.
#
# To use this configuration from test cases, add these lines to the .phpt file:
# --INI--
# defcon.config-file = tests/comma_basic.conf

string	DEFCON_GOOD_STRING = "Good String Content",
	DEFCON_SECOND_STRING = /works/when/unquoted,
	DEFCON_ANOTHER_STRING = 'Same Keyword, Same Type'
