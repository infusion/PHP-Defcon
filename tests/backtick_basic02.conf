# This is defcon configuration file testing backtick substitution
#
# To use this configuration from test cases, add these lines to the .phpt file:
# --INI--
# defcon.config-file = tests/backtick_basic02.conf

string DEFCON_PREFIXED_TICK = "They say: " . `echo hello world`
string DEFCON_EMBEDDED_TICK = "I think " . `echo that` . ' is cool!'
